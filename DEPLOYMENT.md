# 🚀 Інструкція з деплою на GitHub Pages

## Виправлена помилка

**Проблема:** Multiple artifacts named "github-pages" were unexpectedly found

**Рішення:** Об'єднано build і deploy в один job, щоб уникнути створення кількох артефактів.

---

## 📋 Кроки для деплою

### 1. Видаліть попередні невдалі workflow runs (якщо є)

Перейдіть на GitHub:
1. Відкрийте репозиторій `alckemistry_game`
2. Перейдіть у вкладку **Actions**
3. Видаліть всі попередні невдалі запуски (три крапки → Delete workflow run)

### 2. Переконайтеся, що налаштовано GitHub Pages

1. **Settings** → **Pages**
2. **Source**: виберіть **GitHub Actions**
3. Збережіть

### 3. Запуште код

```bash
git add .
git commit -m "Fix deployment workflow"
git push origin main
```

### 4. Перевірте деплой

1. Перейдіть у **Actions** на GitHub
2. Дочекайтеся завершення workflow (зелена галочка ✅)
3. Відкрийте сайт: `https://ВАШ_USERNAME.github.io/alckemistry_game/`

---

## ⚙️ Що було виправлено в `.github/workflows/deploy.yml`:

### До (проблема):
```yaml
jobs:
  build:
    # створює артефакт
  deploy:
    needs: build
    # намагається використати артефакт
```
❌ При кількох запусках створювалося багато артефактів

### Після (виправлено):
```yaml
concurrency:
  group: "pages"
  cancel-in-progress: true  # скасовує попередні запуски

jobs:
  deploy:
    # build і deploy в одному job
```
✅ Тільки один артефакт, попередні запуски скасовуються

---

## 🔧 Налаштування

**`vite.config.ts`:**
```typescript
base: '/alckemistry_game/'  // назва вашого репозиторію
```

**`.github/workflows/deploy.yml`:**
- ✅ Автоматичний деплой при push в `main`
- ✅ Можна запустити вручну (workflow_dispatch)
- ✅ Скасування попередніх запусків

---

## 🐛 Якщо щось не працює

### Білий екран після деплою?
Перевірте `base` у `vite.config.ts` — має співпадати з назвою репозиторію.

### Workflow падає з помилкою?
1. Перевірте, що в Settings → Pages вибрано **GitHub Actions**
2. Видаліть старі workflow runs
3. Запуште знову

### CSS/JS не завантажуються?
Перевірте консоль браузера — можливо неправильний `base` path.

---

## ✅ Готово!

Після успішного деплою ваш додаток буде доступний за адресою:
```
https://ВАШ_USERNAME.github.io/alckemistry_game/
```

🎉 Насолоджуйтесь грою!
